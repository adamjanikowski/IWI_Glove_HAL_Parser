#!/usr/bin/env python

from setuptools import find_packages, setup

setup(name='hal_parser',
      version='0.1.0',
      description='',
      author='',
      author_email='',
      url='',
      packages=find_packages(exclude=['test']),
      scripts=['bin/cli.py']
      )
